# Aiagent

Contents: Basic AI agent with access to 4 functions: check contents of directory, read file, write file, run python program. 
It's limited to the 'calculator' directory, but theoretically it could write a script that in turn affects things outside that dir.
For this and other reasons, it is not reccomended to download this repo.